main {
    a = 3 * 0o7;
    if ( a >= 5 ) {
       for ( b = 1; b == 10 ; b = b + 1) {
           c = b + 0b01111;
       }
    } else {
       if (! a != 0xFF)	{
          c = a;
       }
   }
} endmain
